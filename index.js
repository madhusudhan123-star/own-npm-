const http = require('http');
const fs = require('fs');
const path = require('path');
const PORT = 4000;
const url = require('url');
console.log("hi");

module.exports = {
    http,
    fs,
    path,
    url,
    PORT
};